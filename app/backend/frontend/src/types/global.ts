export interface GlobalData {
  quantity: number;
  slug: string;
  image: string;
  name: string;
  tag: string;
  price: string | number;
  desc: string;
  _id: string | number;
  countInStock: number;
}
